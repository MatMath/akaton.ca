import * as React from 'react';

import {
  Sail, Quality, SailType, CommentsStructure,
} from '../popUpContant/types';

interface Props {
  sailDetail: Sail
}

export const SailDetailsLine = ({ sailDetail }: Props) => (
  <>
    <div>CommentS</div>
  </>
);
